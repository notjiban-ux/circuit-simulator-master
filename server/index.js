/**
 * Diode — 3D Hardware Simulator
 * Express Server (Placeholder)
 *
 * This file is a skeleton for future backend integration.
 * Uncomment and configure when ready to add:
 *   - User authentication (JWT)
 *   - Project save/load via database
 *   - Real-time collaboration (Socket.io)
 */

// const express = require('express');
// const cors = require('cors');
// const path = require('path');
// const authRoutes = require('./routes/auth');
// const projectRoutes = require('./routes/projects');
//
// const app = express();
// const PORT = process.env.PORT || 3001;
//
// // Middleware
// app.use(cors());
// app.use(express.json());
// app.use(express.urlencoded({ extended: true }));
//
// // Serve static frontend files
// app.use(express.static(path.join(__dirname, '..', 'pcb-simulator')));
//
// // API routes
// app.use('/api/auth', authRoutes);
// app.use('/api/projects', projectRoutes);
//
// // Health check
// app.get('/api/health', (req, res) => {
//   res.json({ status: 'ok', app: 'Diode — 3D Hardware Simulator', version: '1.0.0' });
// });
//
// // Catch-all: serve the SPA
// app.get('*', (req, res) => {
//   res.sendFile(path.join(__dirname, '..', 'pcb-simulator', 'index.html'));
// });
//
// app.listen(PORT, () => {
//   console.log(`🔌 Diode server running on http://localhost:${PORT}`);
// });
//
// module.exports = app;

console.log('⚡ Diode server placeholder — uncomment the code above to enable.');
